# Optional: Set this in Netlify Environment Variables if your backend is hosted separately
# Example: https://your-backend-api.onrender.com/api/v1
# If left unset, it defaults to /api/v1 (which is automatically proxied locally by Vite)
VITE_API_URL=
